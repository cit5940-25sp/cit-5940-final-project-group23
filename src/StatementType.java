/**
 * Defines the types of statements in the AST.
 */
public enum StatementType {
    VAR_DECLARATION,
    VAR_ASSIGNMENT,
    PRINT,
    EXPRESSION,
    FUNCTION,
    IF,
    WHILE,
    RUN,
    RETURN
}